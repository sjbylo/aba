apiVersion: v1
baseDomain: {{ base_domain }}
metadata:
  name: {{ cluster_name }}
compute:
- architecture: amd64
  hyperthreading: Enabled
  name: worker
  replicas: {{ num_workers }}
controlPlane:
  architecture: amd64
  hyperthreading: Enabled
  name: master
  replicas: {{ num_masters }}
networking:
  clusterNetwork:
  - cidr: 10.128.0.0/14
    hostPrefix: 23
  machineNetwork:
  - cidr: {{ machine_ip_prefix }}0/{{ prefix_length }}
  networkType: OVNKubernetes
  serviceNetwork:
  - 172.30.0.0/16
platform:
  {%- if num_workers == '0' and num_masters == '1' %}
  none: {}
  {%- else %}
  baremetal:
    apiVIP: "{{ api_vip }}"
    ingressVIP: "{{ ingress_vip}}"
  {%- endif %}
pullSecret: |
  {{ pull_secret|indent(2) }}
  {%- if additional_trust_bundle != '' %}
additionalTrustBundle: |
  {{ additional_trust_bundle|indent(2) }}
  {%- endif %}
sshKey: |
  {{ ssh_key_pub|indent(2) }}
imageContentSources:
{{ image_content_sources }}
