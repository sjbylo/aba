# Aba global configuration file

ocp_channel=		# OpenShift release channel ('stable', 'fast', 'candidate', or 'eus').
ocp_version=		# Target OpenShift version. See https://console.redhat.com/openshift/releases for available versions.

platform=bm			# Deployment platform: 'vmw' (vCenter/ESXi) or 'bm' (bare-metal, skips VM creation). 
pull_secret_file=~/.pull-secret.json	# File path for your Red Hat pull secret, used for image downloads only.
					# Get it from: https://console.redhat.com/openshift/downloads -> Tokens -> Pull-secret.
editor=			# Text editor to use (e.g. nano, vi). Set to 'none' for manual config editing.
ask=true			# Ask for confirmation before major actions (install, delete, etc.).

# Set the following values only if they are known.

op_sets=			# Optional: Comma-separated operator sets (e.g. op_sets=odf,ocpv,acm,mesh3).
				# Operator sets are defined under 'templates/operator-set-*'. Add or edit as needed.
				# Valid values: ocp,acm,ocpv,odf,appdev,mesh2,mesh3,odfdr,quay,sec,ai
				# 'all' includes every operator in redhat-operator-index (ensure sufficient disk space!).

ops=				# Optional: Comma-separated individual operators (e.g. ops=web-terminal,devworkspace-operator).

# Set the following important values for your disconnected network, if known
# When a cluster is created, these values will be included - and can be overridden - in the cluster’s cluster.conf file.

domain={{ domain }}		# Base domain for your OpenShift cluster.
machine_network={{ machine_network }}	# Network segment (CIDR format) where OpenShift will be installed.
dns_servers={{ dns_servers }}		# DNS server IPs (comma-separated).
next_hop_address={{ next_hop_address }}	# Default gateway IP for your private network.
ntp_servers={{ ntp_servers }}		# Important: NTP servers or hostnames (comma-separated). Needed if server clocks aren't synced.
				# Note: Above 5 values can be overridden later in 'cluster.conf'.

#######################################################################################
# ADVANCED SETTINGS — CHANGE ONLY IF REQUIRED

excl_platform=false		# Exclude platform images. Leave 'false' in most cases.

verify_conf=true		# Validate syntax and values in all config files.

oc_mirror_version=v2		# Use version 2 of oc-mirror (v1 is deprecated and unsupported).
				# Key v2 notes (as of Feb 2025):
				# - v2 handles dependencies differently — include all required operators manually.
				#   Example: 'web-terminal' requires 'devworkspace-operator' (included in op. set file: 'templates/operator-set-ocp').
				# - Copy both the image set YAML and TAR files to the bastion (v2 needs the YAML to load images).
				# - Error reporting differs; verify mirrored operators in Quay. Use '--retry' for retries on failure.
				# Reference docs:
				# https://docs.redhat.com/en/documentation/openshift_container_platform/4.18/html/disconnected_environments/mirroring-in-disconnected-environments#about-installing-oc-mirror-v2
				# and:
				# https://docs.openshift.com/container-platform/4.17/disconnected/mirroring/about-installing-oc-mirror-v2.html#oc-mirror-troubleshooting-v2_about-installing-oc-mirror-v2

